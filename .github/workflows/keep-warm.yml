name: Keep Staging Warm

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health
        run: curl -s -o /dev/null -w "%{http_code}" https://medica-be45.onrender.com/health

      - name: Ping frontend
        run: curl -s -o /dev/null -w "%{http_code}" https://frontend-neon-eta-yhbfdcoe8c.vercel.app/login
